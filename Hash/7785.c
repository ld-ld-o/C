#include <stdio.h>
typedef struct Node{
    char key[6];
    char value;
    struct Node* next;
}

int main(void){
    Node* hashTable[52];

    return 0;
}
int hash(const char* name){
    if(name[0]>=


}
